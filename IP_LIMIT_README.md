# IP访问限制功能说明

## 功能概述

本功能实现了基于IP地址的访问速率限制，每个IP地址每天最多可以使用产品**2次**。

## 配置说明

所有配置都**写死在后端代码**中，无需额外配置文件：

- **每日限制次数**: 2次/天
- **数据存储文件**: `ip_rate_limit.json` (项目根目录)
- **限制作用域**: 仅对大纲生成接口 (`/api/outline`) 生效

## 实现细节

### 后端实现

1. **中间件位置**: `backend/middleware/rate_limiter.py`
   - 使用装饰器模式实现
   - 自动获取客户端真实IP（支持反向代理）
   - 使用JSON文件持久化存储访问记录

2. **应用位置**: `backend/routes/api.py`
   - 在 `/api/outline` 接口上应用 `@ip_rate_limit` 装饰器
   - 这是用户生成内容的主要入口点

3. **IP获取优先级**:
   - `X-Forwarded-For` 头（适配Nginx、CDN等反向代理）
   - `X-Real-IP` 头
   - `request.remote_addr`（直连）

### 前端处理

- **错误显示**: 当用户超过限制时，前端会显示友好的错误提示
- **状态码**: 429 (Too Many Requests)
- **响应头**: 包含 `X-RateLimit-Limit` 和 `X-RateLimit-Remaining` 信息

## 数据存储

### 存储文件: `ip_rate_limit.json`

文件格式示例：
```json
{
  "192.168.1.100": {
    "2025-12-05": 2,
    "2025-12-04": 1
  },
  "192.168.1.101": {
    "2025-12-05": 1
  }
}
```

### 数据清理

- 系统自动清理7天前的访问记录
- 每次访问检查时自动触发清理
- 无需手动维护

## 修改限制次数

如需修改每日限制次数，修改以下文件：

**文件**: `backend/middleware/rate_limiter.py`

```python
# 在文件顶部修改这个常量
DAILY_LIMIT = 2  # 改成你想要的次数，如 5、10 等
```

修改后需要重启后端服务。

## 测试

### 运行测试脚本

```bash
# 启动后端服务
cd /path/to/RedInk
uv run python -m backend.app

# 在另一个终端运行测试
python test_ip_limit.py
```

### 手动测试

1. 访问 http://localhost:12398
2. 输入主题并点击"生成大纲"按钮
3. 重复2次后，第3次应该显示限制提示

### 重置测试

删除项目根目录下的 `ip_rate_limit.json` 文件，即可重置所有IP的访问记录。

## 响应示例

### 正常响应

```json
{
  "success": true,
  "outline": "...",
  "pages": [...]
}
```

响应头:
```
X-RateLimit-Limit: 2
X-RateLimit-Remaining: 1
```

### 超过限制

状态码: 429

```json
{
  "success": false,
  "error": "您今天的使用次数已达上限（2次/天），请明天再来！",
  "rate_limit": {
    "daily_limit": 2,
    "remaining": 0
  }
}
```

## 日志输出

后端会记录IP访问日志：

```
INFO - 记录IP访问: 192.168.1.100, 今日已使用 1 次, 剩余 1 次
WARNING - IP 192.168.1.100 今天已达到访问上限 2 次
```

## 注意事项

1. **IP识别**: 
   - 如果使用Nginx等反向代理，确保正确配置 `X-Forwarded-For` 或 `X-Real-IP` 头
   - 局域网内多个用户可能共享同一个公网IP

2. **时区**: 
   - 使用服务器本地时间判断"一天"
   - 每天0点重置计数

3. **数据持久化**:
   - 使用JSON文件存储，轻量级且易于维护
   - 重启服务不会丢失访问记录
   - 可以手动编辑或删除文件

4. **前端无感知**:
   - 前端代码几乎无需修改
   - 错误提示通过现有错误处理机制显示

## 优化建议

如果流量较大，可以考虑：

1. **使用Redis**: 替换JSON文件存储，提高性能
2. **分布式部署**: 使用共享存储（如Redis）同步多个服务器的限制状态
3. **更细粒度**: 针对不同接口设置不同的限制
4. **白名单**: 为特定IP添加白名单功能

## 扩展功能建议

- [ ] 支持用户登录后提高限制
- [ ] VIP用户无限制
- [ ] 管理后台查看IP访问统计
- [ ] 支持黑名单功能
- [ ] 滑动窗口限流算法（更精确）

